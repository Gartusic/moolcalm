<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moolcalm.mapper.MemberMapper">


	<!-- 로그인 -->
	<select id="login" resultType="com.moolcalm.domain.InfoVO">
		select email,password from l_info where email=#{email} and password=#{password}
	</select>
	
	<!-- 출석체크 시에 포인트, 접속횟수를 증가시키고 접속일을 갱신하라는 명령 -->
<!-- 	<update id="dailycheck">
		update l_info set point=point + 100 and login_count = login_count + 1
		and now_date = now() where email= #{email}
	</update> -->
</mapper>